import { LegacyHtmlFragment } from "@/components/site/layout/legacy-html-fragment";

const html = ``;

export function CoworkersViewsView(): React.JSX.Element {
  return <LegacyHtmlFragment html={html} />;
}
